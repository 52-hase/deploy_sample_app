<li class="list-group-item bg-transparent border-0" data-user-id="<%= message.user_id %>">
  <div class="card bg-transparent border-0">
    <div class="card-body">
      <div class="d-flex align-items-start message-row">
        <% if message.user.avatar.attached? %>
          <img class="rounded-circle img-thumbnail" src="<%= url_for(message.user.avatar) %>" style="width: 50px; height: 50px; object-fit: cover;">
        <% else %>
          <div class="bg-secondary text-white rounded-circle d-inline-flex justify-content-center align-items-center" style="width: 50px; height: 50px; font-size: 20px;">
            <%= message.user.name[0].upcase %>
          </div>
        <% end %>

        <div style="max-width: 50%;">
          <div class="text-white">
            <%= message.user.name %>
            <span style="color: rgba(108, 117, 125, 10); margin-left: 0.5rem;"><%= message.created_at.strftime("%Y-%m-%d %H:%M") %></span>
          </div>
          <div class="message-bubble mt-2">
            <div class="card-text" style="color: black; white-space: pre-wrap;"><%= message.content %></div>
            <% if message.image.attached? %>
              <div class="mt-2">
                <img class="img-fluid" src="<%= url_for(message.image) %>" style="width: 300px; height: auto;">
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</li>